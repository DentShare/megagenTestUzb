# Веб-дашборд для мониторинга Megagen Bot

## Установка

1. Установите дополнительные зависимости:
```bash
pip install fastapi uvicorn jinja2
```

Или обновите requirements.txt и установите все зависимости:
```bash
pip install -r requirements.txt
```

## Запуск

1. Убедитесь, что бот запущен и база данных доступна

2. Запустите дашборд:
```bash
uvicorn dashboard:app --host 0.0.0.0 --port 8000 --reload
```

Или используйте Python напрямую:
```bash
python dashboard.py
```

3. Откройте в браузере:
```
http://localhost:8000
```

## Возможности

- 📊 Статистика по заказам в реальном времени
- 👥 Информация о пользователях
- 🏥 Список клиник
- 📈 Графики распределения заказов по статусам
- 📅 График заказов за последние 7 дней
- 🔄 Автообновление каждые 30 секунд

## API Endpoints

- `GET /` - Главная страница дашборда
- `GET /api/stats` - JSON статистика
- `GET /api/orders?limit=50&status=new` - Список заказов
- `GET /api/users` - Список пользователей

## Альтернативные инструменты для мониторинга

### 1. pgAdmin / DBeaver
Для прямого просмотра базы данных PostgreSQL:
- **pgAdmin**: https://www.pgadmin.org/
- **DBeaver**: https://dbeaver.io/

Подключение:
- Host: `localhost` (или ваш DB_HOST)
- Port: `5432` (или ваш DB_PORT)
- Database: `megagen_bot` (или ваш DB_NAME)
- User/Password: из .env файла

### 2. Просмотр логов
Логи сохраняются в файл `bot.log`:
```bash
# Windows PowerShell
Get-Content bot.log -Tail 50 -Wait

# Linux/Mac
tail -f bot.log
```

### 3. Telegram бот
Используйте команды бота для просмотра данных:
- `/report` - генерация отчетов
- `/warehouse` - просмотр активных заказов
- `/menu` - работа с заказами
