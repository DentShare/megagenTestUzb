# Настройка автоматической печати на Xprinter

## Описание

При нажатии кнопки "📦 Собран" на складе автоматически создается этикетка с:
- Номером заказа
- Именем врача
- Названием клиники
- QR кодом

Этикетка автоматически отправляется на настроенный xprinter (если включено).

## Установка зависимостей

```bash
pip install python-escpos
```

Или обновите requirements.txt:
```bash
pip install -r requirements.txt
```

## Настройка в .env

Добавьте следующие переменные в файл `.env`:

```env
# Включить автоматическую печать
PRINTER_ENABLED=true

# Тип подключения: usb, network, serial
PRINTER_TYPE=usb

# Адрес принтера (зависит от типа)
PRINTER_ADDRESS=0x04e8:0x0202

# Для сетевого принтера - порт (по умолчанию 9100)
PRINTER_NETWORK_PORT=9100

# Для serial принтера - скорость (по умолчанию 9600)
PRINTER_SERIAL_BAUDRATE=9600

# Ширина ленты: 58 или 80 мм
PRINTER_WIDTH_MM=58
```

## Типы подключения

### 1. USB принтер

**Формат адреса:** `vendor_id:product_id`

**Как найти vendor_id и product_id:**

**Windows:**
1. Откройте Диспетчер устройств
2. Найдите принтер в разделе "Порты (COM и LPT)" или "Устройства обработки изображений"
3. Правый клик → Свойства → Вкладка "Сведения"
4. Выберите "ИД оборудования" или "Код экземпляра устройства"
5. Найдите значения `VID_XXXX` и `PID_XXXX` (например, `VID_04E8` и `PID_0202`)

**Linux:**
```bash
lsusb
# Найдите принтер, например:
# Bus 001 Device 003: ID 04e8:0202 Samsung Electronics Co., Ltd
# vendor_id = 0x04e8, product_id = 0x0202
```

**Пример настройки:**
```env
PRINTER_TYPE=usb
PRINTER_ADDRESS=0x04e8:0x0202
# или без префикса 0x:
PRINTER_ADDRESS=1256:514
```

**Примечание:** На Linux может потребоваться добавить пользователя в группу `lp`:
```bash
sudo usermod -a -G lp $USER
```

### 2. Сетевой принтер (Network)

**Формат адреса:** IP адрес принтера

**Пример настройки:**
```env
PRINTER_TYPE=network
PRINTER_ADDRESS=192.168.1.100
PRINTER_NETWORK_PORT=9100
```

**Примечание:** 
- Убедитесь, что принтер поддерживает RAW printing (порт 9100)
- Проверьте доступность принтера: `ping 192.168.1.100`
- Убедитесь, что порт открыт: `telnet 192.168.1.100 9100`

### 3. Serial принтер (COM порт)

**Формат адреса:** 
- Windows: `COM1`, `COM2`, и т.д.
- Linux: `/dev/ttyUSB0`, `/dev/ttyS0`, и т.д.

**Пример настройки:**

**Windows:**
```env
PRINTER_TYPE=serial
PRINTER_ADDRESS=COM3
PRINTER_SERIAL_BAUDRATE=9600
```

**Linux:**
```env
PRINTER_TYPE=serial
PRINTER_ADDRESS=/dev/ttyUSB0
PRINTER_SERIAL_BAUDRATE=9600
```

**Примечание:** На Linux может потребоваться добавить пользователя в группу `dialout`:
```bash
sudo usermod -a -G dialout $USER
```

## Ширина ленты

Поддерживаются два размера:
- **58 мм** (по умолчанию) - для большинства термопринтеров
- **80 мм** - для широких принтеров

```env
PRINTER_WIDTH_MM=58
# или
PRINTER_WIDTH_MM=80
```

## Отключение автоматической печати

Если нужно временно отключить автоматическую печать, установите:

```env
PRINTER_ENABLED=false
```

Этикетка все равно будет создаваться и отправляться в Telegram, но на принтер отправляться не будет.

## Проверка работы

1. Убедитесь, что принтер подключен и включен
2. Проверьте настройки в `.env`
3. Перезапустите бота
4. На складе нажмите "📦 Собран" для любого заказа
5. Этикетка должна автоматически отправиться на принтер

## Устранение проблем

### Ошибка: "Библиотека принтера не установлена"
```bash
pip install python-escpos
```

### Ошибка: "Ошибка подключения к USB принтеру"
- Проверьте правильность vendor_id и product_id
- Убедитесь, что принтер подключен
- На Linux: проверьте права доступа (`ls -l /dev/bus/usb/`)

### Ошибка: "Ошибка подключения к сетевому принтеру"
- Проверьте IP адрес: `ping <IP>`
- Проверьте порт: `telnet <IP> 9100`
- Убедитесь, что принтер поддерживает RAW printing

### Ошибка: "Ошибка подключения к serial принтеру"
- Проверьте правильность COM порта
- На Linux: проверьте права доступа (`ls -l /dev/tty*`)
- Проверьте скорость передачи (baudrate)

### Принтер не печатает, но ошибок нет
- Проверьте, что в принтере есть бумага
- Проверьте, что принтер не в режиме "Pause" или "Offline"
- Попробуйте распечатать тестовую страницу с принтера

## Логирование

Все операции с принтером логируются. Проверьте логи бота для диагностики:

```bash
tail -f bot.log | grep -i printer
```

## Дополнительная информация

- Библиотека python-escpos: https://github.com/python-escpos/python-escpos
- Документация ESC/POS: https://reference.epson-biz.com/

