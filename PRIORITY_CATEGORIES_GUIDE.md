# Руководство по настройке приоритетных категорий и линеек

## Описание

Система позволяет разделить категории и линейки на **приоритетные** (показываются сразу) и **дополнительные** (скрыты в кнопке "Дополнительно"). Это помогает упростить интерфейс и сделать его более удобным для пользователей.

## Настройка в .env файле

Добавьте следующие переменные в файл `.env`:

```env
# Приоритетные категории (показываются сразу, остальные в "Дополнительно")
# Разделяйте запятыми
PRIORITY_CATEGORIES=Импланты,Протетика

# Приоритетные линейки (для категории "Импланты", остальные в "Дополнительно")
# Разделяйте запятыми
PRIORITY_LINES=AnyOne,AO Onestage

# Приоритетные товары для Протетики (остальные в "Дополнительно")
PRIORITY_PROSTHETICS_PRODUCTS=EZ Post Abutment,Multi Unit Abutment

# Приоритетные товары для Лаборатории (остальные в "Дополнительно")
PRIORITY_LABORATORY_PRODUCTS=Товар1,Товар2

# Приоритетные товары для Наборов (остальные в "Дополнительно")
PRIORITY_SETS_PRODUCTS=Набор1,Набор2
```

## Как это работает

### Категории

1. **Приоритетные категории** (из `PRIORITY_CATEGORIES`) показываются сразу в основном меню
2. **Остальные категории** скрыты в кнопке "📦 Дополнительно (N)", где N - количество дополнительных категорий
3. При нажатии на "Дополнительно" показываются все категории

**Пример:**
- Если у вас категории: `Импланты`, `Протетика`, `Лаборатория`, `Наборы`, `материалы`
- И в `.env` указано: `PRIORITY_CATEGORIES=Импланты,Протетика`
- То в меню будут показаны:
  ```
  [Импланты]
  [Протетика]
  [📦 Дополнительно (3)]
  [🛒 Корзина]
  ```

### Линейки (для категории "Импланты")

1. **Приоритетные линейки** (из `PRIORITY_LINES`) показываются сразу
2. **Остальные линейки** скрыты в кнопке "📦 Дополнительно (N)"
3. При нажатии на "Дополнительно" показываются все линейки

**Пример:**
- Если у вас линейки: `AnyOne`, `AO Onestage`, `AO Short SP`, `AOO Deep`, `AOOL Deep`, `AR MX`
- И в `.env` указано: `PRIORITY_LINES=AnyOne,AO Onestage`
- То в меню будут показаны:
  ```
  [AnyOne]
  [AO Onestage]
  [📦 Дополнительно (4)]
  [⬅ Назад] [🛒 Корзина]
  ```

## Настройка для категорий Протетика, Лаборатория и Наборы

Для категорий **Протетика**, **Лаборатория** и **Наборы** можно настроить приоритетные **товары (виды)**, а не линейки:

- `PRIORITY_PROSTHETICS_PRODUCTS` - приоритетные товары для Протетики
- `PRIORITY_LABORATORY_PRODUCTS` - приоритетные товары для Лаборатории  
- `PRIORITY_SETS_PRODUCTS` - приоритетные товары для Наборов

**Пример:**
- Если у вас в Протетике товары: `EZ Post Abutment`, `Multi Unit Abutment`, `Custom Abutment`, `Temporary Abutment`
- И в `.env` указано: `PRIORITY_PROSTHETICS_PRODUCTS=EZ Post Abutment,Multi Unit Abutment`
- То в меню будут показаны:
  ```
  [EZ Post Abutment]
  [Multi Unit Abutment]
  [📦 Дополнительно (2)]
  [⬅ Назад] [🛒 Корзина]
  ```

Для категории **материалы** приоритетные товары не применяются, так как обычно там мало товаров.

## Отключение функции

Если вы хотите показывать все категории/линейки сразу (без группировки):

1. **Вариант 1:** Оставьте переменные пустыми:
   ```env
   PRIORITY_CATEGORIES=
   PRIORITY_LINES=
   ```

2. **Вариант 2:** Удалите переменные из `.env` (будут использованы значения по умолчанию)

## Рекомендации

1. **Выбирайте 2-3 самые популярные категории** в приоритетные
2. **Выбирайте 2-4 самые популярные линейки** в приоритетные
3. **Не делайте слишком много приоритетных** - цель упростить интерфейс
4. **Периодически пересматривайте** приоритеты на основе статистики заказов

## Пример полной настройки

```env
# Приоритетные категории
PRIORITY_CATEGORIES=Импланты,Протетика

# Приоритетные линейки для имплантов
PRIORITY_LINES=AnyOne,AO Onestage,AnyRidge

# Приоритетные товары для Протетики
PRIORITY_PROSTHETICS_PRODUCTS=EZ Post Abutment,Multi Unit Abutment

# Приоритетные товары для Лаборатории
PRIORITY_LABORATORY_PRODUCTS=Товар1,Товар2

# Приоритетные товары для Наборов
PRIORITY_SETS_PRODUCTS=Набор1,Набор2

# Количество кнопок в строке
CATEGORY_BUTTONS_PER_ROW=1
LINE_BUTTONS_PER_ROW=1
```

## Технические детали

- Настройки загружаются из `.env` при старте бота
- Изменения в `.env` требуют перезапуска бота
- Если категория/линейка указана в приоритетных, но отсутствует в каталоге, она игнорируется
- Если все категории/линейки указаны как приоритетные, кнопка "Дополнительно" не показывается
